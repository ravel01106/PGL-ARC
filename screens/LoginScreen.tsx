import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LoginScreen = () => {
  return (
    <View>
      <Text>Tu pinche madre pelotudo</Text>
    </View>
  );
};

export default LoginScreen;

const styles = StyleSheet.create({});
